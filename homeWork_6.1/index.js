const user = {
    name: "Anton",
    age: 20,
    address: "Kiyv",

    getInfoUser() {
        console.log(`User: ${user.name}, age: ${user.age}, address: ${user.address}.`);
    }
};

user.getInfoUser();

